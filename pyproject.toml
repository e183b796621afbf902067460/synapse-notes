[project]
name = "synapse-notes"
version = "0.0.1"
description = "..."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.12, <3.15"

[tool.uv]
package = false

# https://docs.astral.sh/uv/concepts/projects/workspaces/#workspace-sources
[tool.uv.sources]
# .pi/
messoyakha-pi = { workspace = false }

[tool.uv.workspace]
members = [
    ".pi/",
]
